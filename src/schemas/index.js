import api from '../api.json';

import {normalize, schema} from 'normalizr';

const media = new shema.Entity();

const category = new shema.Entity();

const categories = 

const normalizeData = normalize(api, categories);

export default normalizeData;